spring:
  ### Database ###
  datasource:
    #url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    url: jdbc:h2:tcp://localhost:9092/mem:testdb;MVCC=true
    username: root
    password: 1367
    validation-query: SELECT 1
    continue-on-error: true
    hikari:
      jdbc-url: jdbc:h2:tcp://localhost:9092/mem:testdb;MVCC=true
    #initialization-mode: NEVER
  h2:
    console:
      enabled: true
      path: /h2-console
  ### JPA ###
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        query:
          in_clause_parameter_padding: true
          #plan_cache_max_size: 2048
          #plan_parameter_metadata_max_size: 128
        format_sql: true
        use_sql_comments: true
        show_sql: true
  flyway:
    enabled: false
    locations: classpath:db/migration/${spring.profiles.active}

  ### View Resolver ###
  thymeleaf:
    enabled: true
    mode: HTML

  ### Redis for websocket session ###
  redis:
    host: localhost
    port: 6379

### logging - hibernate ###
logging:
  level:
    org:
      hibernate:
        sql: trace
        type:
          descriptor:
            sql: trace
---
spring:
  profiles: embeddedRedis
  redis:
    host: localhost
    port: 6379
---
spring:
  profiles: remoteRedis
  redis:
    host: 90.90.90.1
    port: 6379
